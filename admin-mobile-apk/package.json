{
  "name": "admin-mobile-apk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "apk:debug": "npm run build && npm run cap:sync && npm run cap:open"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.0",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/core": "^8.3.0",
    "@capacitor/local-notifications": "^8.0.2",
    "@supabase/supabase-js": "^2.103.0"
  }
}
